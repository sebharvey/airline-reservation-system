name: Schedule API - Build

on:
  push:
    branches: [ main, master ]
    paths:
      - 'src/API/Microservices/ReservationSystem.Microservices.Schedule/**'
      - 'src/API/ReservationSystem.sln'
      - '.github/workflows/schedule-api-build.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'src/API/Microservices/ReservationSystem.Microservices.Schedule/**'
      - 'src/API/ReservationSystem.sln'
      - '.github/workflows/schedule-api-build.yml'

jobs:
  build:
    name: Build Schedule API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/API/Microservices/ReservationSystem.Microservices.Schedule/ReservationSystem.Microservices.Schedule.csproj

      - name: Build
        run: dotnet build src/API/Microservices/ReservationSystem.Microservices.Schedule/ReservationSystem.Microservices.Schedule.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish src/API/Microservices/ReservationSystem.Microservices.Schedule/ReservationSystem.Microservices.Schedule.csproj --configuration Release --no-build --output ./publish/schedule-api

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: schedule-api
          path: ./publish/schedule-api
          retention-days: 7
