name: Identity API - Build

on:
  push:
    branches: [ main, master ]
    paths:
      - 'src/API/Microservices/ReservationSystem.Microservices.Identity/**'
      - 'src/API/ReservationSystem.sln'
      - '.github/workflows/identity-api-build.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'src/API/Microservices/ReservationSystem.Microservices.Identity/**'
      - 'src/API/ReservationSystem.sln'
      - '.github/workflows/identity-api-build.yml'

jobs:
  build:
    name: Build Identity API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/API/Microservices/ReservationSystem.Microservices.Identity/ReservationSystem.Microservices.Identity.csproj

      - name: Build
        run: dotnet build src/API/Microservices/ReservationSystem.Microservices.Identity/ReservationSystem.Microservices.Identity.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish src/API/Microservices/ReservationSystem.Microservices.Identity/ReservationSystem.Microservices.Identity.csproj --configuration Release --no-build --output ./publish/identity-api

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: identity-api
          path: ./publish/identity-api
          retention-days: 7
